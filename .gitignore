node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vercel
.vinxi
__unconfig*
todos.json

# Supabase CLI local state — machine-specific link info (project ref, pooler
# connection string), not project config. Never commit.
supabase/.temp/
